let altura_cm = 170;

let altura_m = 1.70;

let peso_kg = 90.7;


let alt_m_roundup = Math.round(altura_m);
console.log(alt_m_roundup);

let peso_kg_roundDown = Math.floor(peso_kg);
console.log(peso_kg_roundDown);

let valMaxJS = (Number.MAX_VALUE) + 1;
console.log(valMaxJS);